# UnityProject

这里预留给后续 Unity 版本工程使用。

当前环境没有检测到 Unity Editor，所以还没有生成真实 Unity 工程文件。后续使用 Unity Hub 创建项目时，请选择本目录作为工程目录，模板选择 `2D`，并按根目录的 `UNITY_PORT_PLAN.md` 完整复刻 HTML 版内容。

约定：

- 不覆盖根目录的 HTML 版。
- 资源优先从 `../resource` 导入 Unity。
- 第一阶段先完成单机同内容版本。
- 第二阶段再做局域网联机。
